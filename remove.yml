---
# Upgrade the fleet
- name: Remove Packages
  hosts: all:!unmanaged

  roles:
    - role: group_by

- hosts: yum
  sudo: yes
  tasks:
    - command: yum erase -y {{ item }}
      with_items: remove

- hosts: apt
  sudo: yes
  tasks:
    - command: apt-get purge -y {{ item }}
      with_items: remove
