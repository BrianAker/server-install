# vim:ft=yaml
---
- pip:  requirements=pip/ansible.txt
        virtualenv_command="{{ virtualenv_bin }}"
        virtualenv="~/.python"
        state=present
  sudo: yes
  sudo_user: "{{ deploy_username }}"
