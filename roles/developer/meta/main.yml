---
allow_duplicates: yes
dependencies:
  - { role: adduser, username: "{{ developer_username }}", is_admin: false, has_authorized_key: true }
  - { role: bzr, username: "{{ developer_username }}" }

