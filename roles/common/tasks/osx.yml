---

- name: install common osx software
  ignore_errors: yes
  homebrew: name={{ item }} state=present
  with_items:
    - bzr
    - curl
    - git
    - make
    - pkg-config
    - pwgen
    - ssh-copy-id
    - wget
