---

- homebrew: name=pkg-config
            state=present
  ignore_errors: yes
  with_items:
    - pkg-config
    - pwgen
    - ssh-copy-id

- homebrew: name=bzr
            state=absent
  ignore_errors: yes

#    - bind
#    - curl
#    - git
#    - gpg2
#    - make

#- name: accept xcode license
#  sudo: yes
#  command: xcodebuild -license
