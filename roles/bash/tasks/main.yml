---
- file: src=/usr/local/bin/{{ item }}
        dest=/bin/{{ item }}
        owner=root
        state=file
  with_first_found:
    - /usr/bin/bash
    - /usr/local/bin/bash
  when: "ansible_distribution == 'FreeBSD'"
