---
- command: "echo ~{{ User.name }}"
  register: homedir

- debug:
    var: homedir

- set_fact: 
    user_homedir: "{{ homedir.stdout }}"
  when: homedir.stdout

- set_fact: 
    profile_d: "{{ user_homedir }}/.profile.d"

