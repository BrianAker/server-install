# vim:ft=sh

if [ -z "$CUSTOMRC_INCLUDED" ]
then
  CUSTOMRC_INCLUDED=yes

  if [ -d "$HOME/.profile.d" ]; then
    for i in "$HOME/.profile.d/*.sh" ; do
      if [ -r "$i" ]; then
        if [ "${-#*i}" != "$-" ]; then
          . "$i"
        else
          . "$i" >/dev/null
        fi
      fi
    done
  fi

  if [ ! -v PS1 ]; then
    VISUAL=vi
    EDITOR=vi
  fi
fi
