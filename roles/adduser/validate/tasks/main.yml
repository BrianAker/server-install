---
- debug: msg="Adding user {{ User.name }}"

- fail: "User was not defined."
  when: User is not defined

- fail: "User.name was not defined."
  when: User.name is not defined

- fail: "default_shell was not defined."
  when: default_shell is not defined
