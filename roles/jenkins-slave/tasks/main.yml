---
- name: create jenkins user
  action: user name=jenkins state=present

- name: Add the CI key
  action: user name=ansible state=present

- name: Add keys for CI user
  sudo: yes
  sudo_user: jenkins
  raw: ssh-import-id d-ci
