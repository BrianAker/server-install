---

- name: create default hosts.allow
  sudo: yes
  action: copy src=hosts.allow dest=/etc/hosts.allow

- name: create default hosts.deny
  sudo: yes
  action: copy src=hosts.deny dest=/etc/hosts.deny
