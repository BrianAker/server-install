{% if syslog_remote_log is defined %}
{{ syslog_remote_log }}
{
    sharedscripts
    postrotate
        /bin/kill -HUP `cat /var/run/syslogd.pid 2> /dev/null` 2> /dev/null || true
    endscript
}
{% endif %}
