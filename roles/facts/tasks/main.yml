---
- command: Obtain the group assigned to the root user.
  register: id_group_result

- set_fact: root_group="{{ id_group_result.stdout }}"

- command: pwd
  register: srcdir
  connection: local
  sudo: no

- set_fact: srcdir="{{ srcdir.stdout }}"
