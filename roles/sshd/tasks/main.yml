---
- include_vars: "{{ item }}"
  with_first_found:
    - files:
        - "{{ ansible_distribution }}.yml"

- include: adduser.yml
  when: sshd_allow_username is defined

- include: harden.yml
  when: sshd_harden == True
