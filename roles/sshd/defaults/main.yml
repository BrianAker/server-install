--- # vim:ft=yaml
sshd_packages:
  FreeBSD: []

  MacOSX: []

  RedHat:
    - openssh-server

  Fedora:
    - openssh-server

  CentOS:
    - openssh-server

  Debian:
    - openssh-server

  Ubuntu:
    - openssh-server
