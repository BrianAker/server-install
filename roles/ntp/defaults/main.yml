---
ntp_service:
  CentOS: ntpd
  Debian: ntp
  Fedora: ntpd
  FreeBSD: ntpd
  MacOSX: ntp
  RedHat: ntpd
  Ubuntu: ntp

ntp_packages:
  FreeBSD:
    - ntp
  RedHat:
    - ntpdate
    - ntp
  Debian:
    - ntpdate
    - ntp

  MacOSX:
    - pkg-config
    - pwgen
    - ssh-copy-id

  Fedora: []

  CentOS: []

  Ubuntu: []
