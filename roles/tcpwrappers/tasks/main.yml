---
- name: create default hosts allow and deny files
  sudo: yes
  template: src="{{ item }}.j2"
            dest="/etc/{{ item }}"
            owner=root
            group="{{ root_group }}"
            mode=644
            force=yes
  with_items:
    - hosts.allow
    - hosts.deny
