---
- name: install local pip packages for user
  sudo: yes
  sudo_user: "{{ User.name }}"
  pip:  name="{{ item }}"
        virtualenv_command="{{ virtualenv_bin }}"
        virtualenv="{{ User.home }}/{{ pip_user_dir }}"
        state=present
  with_items: pip_packages
