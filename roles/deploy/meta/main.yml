---
allow_duplicates: no
dependencies:
  - { role: sshd }
  - role: adduser
    User:
      name: "{{ deploy_username }}"
      shell: "{{ default_shell }}"
      keychain: True
      is_admin: True
      is_remote: True
  - { role: python }
