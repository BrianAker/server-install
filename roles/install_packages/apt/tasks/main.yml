---
- name: "install packages with apt for {{ ansible_distribution }}"
  sudo: yes
  apt:  update_cache=yes
        cache_valid_time=3600
        pkg={{ item }}
        state={{ default_package_state }}
  with_items: packages[ansible_distribution]
  when: packages is defined and packages[ansible_distribution] is defined
  tags: package_install
