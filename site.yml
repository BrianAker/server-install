---

# Root ansible playbook
- hosts: all
  roles:
    - base
    - setup
    - firewalld

- hosts: localhost
  roles:
    - base
    - setup
    - firewalld

- include: mailman.yml
- include: dnsservers.yml
- include: webservers.yml
- include: jenkins-slaves.yml
