---
- hosts: FreeBSD
  name: Bootstrap
  gather_facts: no

  roles:
    - { role: upgrade/FreeBSD, want_upgrade: False }
    - resolv
    - bootstrap/FreeBSD

# Rulebook used for bootstrapping a system.
- hosts: all

  roles:
    - { role: bootstrap, want_upgrade: True }

- include: harden.yml
