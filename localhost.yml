---
- name: Localhost
  hosts: localhost
  connection: local

  vars_files:
     - "vars/{{ ansible_os_family }}"
     - "vars/{{ ansible_distribution }}"
     - "vars/Defaults"

  roles:
    - build
    - { role: developer, developer_username: brian, keychain: True, is_admin: True }
