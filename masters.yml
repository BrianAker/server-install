# Host rules for Jenkins-ci masters
- name: Jenkins Master
  hosts: masters

  vars_files:
     - "vars/{{ ansible_os_family }}"
     - "vars/{{ ansible_distribution }}"
     - "vars/Defaults"

  roles:
    - bazaar
    - build
    - jenkins
    - pound
    - sshd
    - tcpwrappers
