# Host rules for Jenkins-ci slaves
- name: Jenkins Slave
  hosts: slaves

  roles:
    - build
    - role: jenkins-slave
      User:
        name: jenkins
        shell: "{{ default_shell }}"
        is_remote: True
