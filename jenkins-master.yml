---
# Host rules for Jenkins-ci masters
- hosts: masters
  name: Jenkins Master
  roles:
    - { role: adduser, username: brian, is_admin: True, has_authorized_key: False }
    - build
    - sshd
    - pound
    - jenkins-master
    - tcpwrappers
    - bazaar
